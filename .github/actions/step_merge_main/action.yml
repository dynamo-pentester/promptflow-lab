name: 'Step Merge Main'
description: 'Merges main into current branch'
runs:
  using: composite
  steps:
    - name: merge
      shell: bash
      run: |
        git fetch origin main
        git merge origin/main --no-edit || true
        echo "Merged cleanly"
